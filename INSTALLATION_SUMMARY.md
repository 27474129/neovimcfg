# Установка Neovim конфигурации завершена! ✓

## Что установлено:

### Основное
- ✓ Neovim v0.11.5
- ✓ vim-plug (менеджер плагинов)
- ✓ Конфиг init.vim скопирован в ~/.config/nvim/
- ✓ 29 плагинов установлено

### Инструменты
- ✓ ripgrep (быстрый поиск)
- ✓ fd-find (поиск файлов)
- ✓ xclip (поддержка буфера обмена)
- ✓ unzip
- ✓ Hack Nerd Font (шрифт с иконками)

### Языки программирования
- ✓ Python 3 + pip
- ✓ Node.js v12 + npm
- ✓ Rust + Cargo (rustc 1.91.1)
- ✓ Go 1.18 (установлен, но старая версия)

### LSP серверы
- ✓ pyright (Python LSP)
- ✓ typescript-language-server
- ⚠ gopls - НЕ установлен (требуется Go 1.24+, а у вас Go 1.18)
- ⚠ rust-analyzer - команда доступна, но нужна проверка
- ⚠ staticcheck - НЕ установлен (требуется более новая версия Go)
- ⚠ golangci-lint - не установлен
- ⚠ delve (Go debugger) - не установлен

### Python пакеты
- ✓ debugpy (отладчик Python)

### Treesitter парсеры
Установлено 13 парсеров:
- ✓ python, go, typescript, javascript
- ✓ rust, lua, vim, vimdoc, query, c
- ✓ gomod, gosum, gowork

## Как запустить Neovim:

```bash
# Обновите переменные окружения
source ~/.bashrc

# Запустите neovim
nvim

# Или откройте файл
nvim test.py
```

## Основные горячие клавиши (из конфига):

### Навигация
- `Ctrl+n` - Открыть/закрыть файловое дерево (NvimTree)
- `Ctrl+h/l/j` - Перемещение между окнами
- `Tab` - Следующий буфер
- `o` - Предыдущий буфер
- `x` - Закрыть текущий буфер

### Поиск
- `F4` - Поиск файлов (Telescope)
- `F2` - Поиск по содержимому (live grep)
- `F3` - Список открытых буферов

### LSP
- `gd` - Перейти к определению
- `q` - Показать информацию о символе
- `t` - Переименовать символ
- `f` - Найти использования
- `Space+e` - Показать ошибки

### Git
- `F1` - Git blame (кто написал эту строку)

### Отладка (DAP)
- `Ctrl+b` - Поставить/убрать точку останова
- `ds` - Начать отладку
- `dc` - Продолжить выполнение
- `dn` - Шаг вперед
- `di` - Войти в функцию
- `do` - Выйти из функции
- `de` - Завершить отладку

### Другое
- `jj` - Выход из режима вставки (вместо Esc)
- `Ctrl+z` - Отменить действие
- `Ctrl+y` - Повторить действие
- `Ctrl+c` - Копировать (в визуальном режиме)
- `Ctrl+v` - Вставить
- `Ctrl+a` - Выход из Neovim

## Что нужно доустановить (опционально):

### Если нужна полная поддержка Go:

1. Обновите Go до версии 1.24+:
```bash
# Удалите старую версию
sudo rm -rf /usr/lib/go-1.18

# Скачайте и установите новую (например, 1.21+)
GO_VERSION="1.22.0"
wget "https://go.dev/dl/go${GO_VERSION}.linux-amd64.tar.gz"
sudo rm -rf /usr/local/go
sudo tar -C /usr/local -xzf "go${GO_VERSION}.linux-amd64.tar.gz"
rm "go${GO_VERSION}.linux-amd64.tar.gz"

# Обновите PATH
export PATH=$PATH:/usr/local/go/bin:$HOME/go/bin
source ~/.bashrc
```

2. Затем установите Go инструменты:
```bash
go install golang.org/x/tools/gopls@latest
go install honnef.co/go/tools/cmd/staticcheck@latest
go install github.com/go-delve/delve/cmd/dlv@latest
go install golang.org/x/tools/cmd/goimports@latest
curl -sSfL https://raw.githubusercontent.com/golangci/golangci-lint/master/install.sh | sh -s -- -b $(go env GOPATH)/bin
```

### Если нужен Rust LSP:
```bash
rustup component add rust-analyzer
```

## Проверка установки:

```bash
# Проверьте версии
nvim --version
python3 --version
node --version
rustc --version
go version

# Проверьте LSP серверы
which pyright
which typescript-language-server
which rust-analyzer
which gopls

# Проверьте плагины
ls ~/.config/nvim/plugged/

# Проверьте Treesitter парсеры
ls ~/.config/nvim/plugged/nvim-treesitter/parser/
```

## Запуск демо файлов:

В репозитории есть демо файлы с киберпанк темой:
```bash
nvim /opt/neovimcfg/cyberpunk_demo.py
nvim /opt/neovimcfg/blade_runner_demo.py
```

## Тема:

По умолчанию установлена тема **Blade Runner Neon** (черный фон с неоновыми синими и красными цветами).

Чтобы переключить тему, отредактируйте `~/.config/nvim/init.vim`:
- Строка 87: `colorscheme tokyonight` (можно изменить на `catppuccin`, `gruvbox`, `material`)

---

**Установка завершена! Приятного использования Neovim! 🚀**
