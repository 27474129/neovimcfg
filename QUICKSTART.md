# ⚡ Quick Start - Установка за 30 секунд

## 1️⃣ Установка

### Linux:
```bash
cd /opt/neovimcfg && chmod +x install_linux.sh && ./install_linux.sh
```

### macOS:
```bash
cd /opt/neovimcfg && chmod +x install_macos.sh && ./install_macos.sh
```

Скрипт всё сделает автоматически! ☕ Подожди 2-3 минуты.

---

## 2️⃣ Запуск

```bash
nvim
```

---

## 3️⃣ Первые шаги

1. **Открой файловое дерево**: нажми `Ctrl+n`
2. **Поиск файлов**: нажми `F4`
3. **Поиск по тексту**: нажми `F2`
4. **Выход**: нажми `Ctrl+a`

---

## 4️⃣ Основные клавиши

```
Ctrl+n  - файловое дерево
F4      - поиск файлов
F2      - поиск по тексту
gd      - перейти к определению (в коде)
q       - инфо о символе (в коде)
jj      - выход из insert режима (вместо Esc)
Tab     - следующий буфер
```

---

## 5️⃣ Готово!

Полная документация в `README.md`

**Приятного кодинга! 🚀**

