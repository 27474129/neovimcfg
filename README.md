# 🚀 Neovim Cyberpunk Config - Установка одной кнопкой

Автоматизированная установка Neovim с киберпанк темой, LSP серверами, автодополнением, файловым деревом, поиском и отладчиком для Python, Go, TypeScript, Rust.

## 🎯 Установка одной командой

### Linux (Ubuntu/Debian/Fedora/Arch)

```bash
cd /opt/neovimcfg && chmod +x install_linux.sh && ./install_linux.sh
```

### macOS

```bash
cd /opt/neovimcfg && chmod +x install_macos.sh && ./install_macos.sh
```

Скрипт автоматически:
- ✓ Установит Neovim и все зависимости
- ✓ Установит LSP серверы (Python, TypeScript, Go, Rust)
- ✓ Установит все плагины (29 плагинов)
- ✓ Установит Treesitter парсеры для подсветки синтаксиса
- ✓ Установит Hack Nerd Font (для иконок)
- ✓ Настроит конфиг

**Все автоматически, без ручных действий!**

---

## 🎨 Что установится?

### Основное
- **Neovim** - современный редактор
- **vim-plug** - менеджер плагинов
- **32 плагина** - полный набор для разработки

### Плагины
- **Telescope** - быстрый поиск файлов и текста
- **NvimTree** - файловое дерево
- **LSP** - автодополнение и навигация по коду
- **Treesitter** - умная подсветка синтаксиса
- **nvim-cmp** - автодополнение
- **gitsigns** - git интеграция
- **bufferline** - красивые табы
- **lualine** - статусная строка
- **nvim-dap** - отладчик (debugger)
- **Comment.nvim** - комментирование кода
- **autoclose** - автозакрытие скобок

### Темы
- **Blade Runner Neon** (по умолчанию) - черный фон с неоновыми синими и красными цветами
- Tokyo Night, Catppuccin, Gruvbox, Material (можно переключить)

### LSP серверы
- **pyright** - Python
- **typescript-language-server** - TypeScript/JavaScript
- **gopls** - Go (требует Go 1.22+)
- **rust-analyzer** - Rust

### Инструменты
- **ripgrep** - быстрый поиск
- **fd** - быстрый поиск файлов
- **debugpy** - отладчик Python
- **delve** - отладчик Go (если установлен Go)

---

## ⌨️ Горячие клавиши

### Навигация
| Клавиша | Действие |
|---------|----------|
| `Ctrl+n` | Открыть/закрыть файловое дерево (NvimTree) |
| `Ctrl+h/l/j` | Перемещение между окнами |
| `Tab` | Следующий буфер |
| `o` | Предыдущий буфер |
| `x` | Закрыть текущий буфер |

### Поиск (Telescope)
| Клавиша | Действие |
|---------|----------|
| `F4` | Поиск файлов |
| `F2` | Поиск по содержимому (live grep) |
| `F3` | Список открытых буферов |

### LSP (Навигация по коду)
| Клавиша | Действие |
|---------|----------|
| `gd` | Перейти к определению |
| `q` | Показать информацию о символе (hover) |
| `t` | Переименовать символ |
| `f` | Найти использования (references) |
| `Space+e` | Показать ошибки |

### Git
| Клавиша | Действие |
|---------|----------|
| `F1` | Git blame (кто написал эту строку) |
| `F1` (visual) | Git blame для выделенного блока |

### Отладка (DAP)
| Клавиша | Действие |
|---------|----------|
| `Ctrl+b` | Поставить/убрать точку останова |
| `ds` | Начать отладку |
| `dc` | Продолжить выполнение |
| `dn` | Шаг вперед (step over) |
| `di` | Войти в функцию (step into) |
| `do` | Выйти из функции (step out) |
| `de` | Завершить отладку |

### Редактирование
| Клавиша | Действие |
|---------|----------|
| `jj` | Выход из режима вставки (вместо Esc) |
| `Ctrl+z` | Отменить действие (undo) |
| `Ctrl+y` | Повторить действие (redo) |
| `Ctrl+c` (visual) | Копировать в системный буфер |
| `Ctrl+v` | Вставить из системного буфера |
| `cy` | Скопировать в системный буфер |
| `cp` | Вставить из системного буфера |

### Другое
| Клавиша | Действие |
|---------|----------|
| `Ctrl+a` | Выход из Neovim |
| `Ctrl+k` | Закрыть все буферы кроме текущего |

---

## 🎬 Быстрый старт

После установки просто запусти:

```bash
nvim
```

Или открой файл:

```bash
nvim test.py
```

### Первое использование

1. **Откройся файловое дерево**: `Ctrl+n`
2. **Попробуй поиск файлов**: `F4`
3. **Попробуй поиск по тексту**: `F2`
4. **Наведи на код и нажми** `q` - увидишь документацию
5. **Перейди к определению**: `gd`

---

## 🎨 Смена темы

По умолчанию используется **Blade Runner Neon Theme** (черный + неоновый синий + красный).

Чтобы изменить тему, отредактируй `~/.config/nvim/init.vim` (строка ~87):

```vim
colorscheme tokyonight
```

Доступные темы:
- `tokyonight` - Blade Runner Neon (по умолчанию)
- `catppuccin`
- `gruvbox`
- `material`
- `tokyodark`

---

## 🔧 Устранение неполадок

### Linux: Nvim не видит конфиг

Если видишь стандартный nvim без темы:

```bash
# Проверь где nvim ищет конфиг
nvim --headless -c 'echo stdpath("config")' -c 'qall'

# Если показывает /home/root вместо /root, скопируй конфиг:
mkdir -p /home/root/.config/nvim
cp -r /root/.config/nvim/* /home/root/.config/nvim/

# Или создай симлинк
ln -s /root/.config/nvim /home/root/.config/nvim
```

### Go: gopls не устанавливается

Требуется Go 1.22+. Обнови Go:

```bash
# Скачай новую версию
GO_VERSION="1.22.0"
wget "https://go.dev/dl/go${GO_VERSION}.linux-amd64.tar.gz"
sudo rm -rf /usr/local/go
sudo tar -C /usr/local -xzf "go${GO_VERSION}.linux-amd64.tar.gz"
rm "go${GO_VERSION}.linux-amd64.tar.gz"

# Обнови PATH
export PATH=$PATH:/usr/local/go/bin:$HOME/go/bin
source ~/.bashrc

# Установи gopls
go install golang.org/x/tools/gopls@latest
```

### macOS: Плагины не работают

```bash
# Переустанови плагины
nvim +PlugClean +PlugInstall +qall

# Переустанови Treesitter парсеры
nvim +TSUpdate +qall
```

---

## 📦 Что внутри?

### Структура конфига

```
~/.config/nvim/
├── init.vim          # Главный конфиг
└── plugged/          # Установленные плагины
    ├── telescope.nvim
    ├── nvim-tree.lua
    ├── nvim-lspconfig
    ├── nvim-treesitter
    └── ... (29 плагинов)
```

### Демо файлы

В репозитории есть демо файлы для проверки темы:

```bash
nvim /opt/neovimcfg/cyberpunk_demo.py
nvim /opt/neovimcfg/blade_runner_demo.py
```

---

## 🆘 Поддержка

Если что-то не работает:

1. **Проверь версию Neovim**: `nvim --version` (нужна 0.9+)
2. **Проверь плагины**: `:PlugStatus` внутри nvim
3. **Проверь LSP**: `:LspInfo` внутри nvim
4. **Переустанови плагины**: `:PlugClean` затем `:PlugInstall`
5. **Посмотри логи**: `:messages` внутри nvim

---

## 🚀 Продвинутое использование

### Команды плагинов

```vim
" Telescope
:Telescope find_files    " Поиск файлов
:Telescope live_grep     " Поиск по тексту
:Telescope buffers       " Список буферов

" NvimTree
:NvimTreeToggle         " Открыть/закрыть дерево
:NvimTreeFocus          " Фокус на дерево

" LSP
:LspInfo                " Инфо об LSP серверах
:LspRestart             " Перезапустить LSP

" Treesitter
:TSInstall python       " Установить парсер для Python
:TSUpdate               " Обновить все парсеры
```

### Отладка Python

1. Открой файл: `nvim test.py`
2. Поставь точку останова: `Ctrl+b`
3. Запусти отладку: `ds`
4. Управляй: `dc` (continue), `dn` (next), `di` (step into)

---

## 📝 Changelog

### Последние изменения

- ✓ Автоматическая установка плагинов
- ✓ Автоматическая установка Treesitter парсеров
- ✓ Исправлены ошибки загрузки (pcall для всех плагинов)
- ✓ Поддержка Linux и macOS
- ✓ Blade Runner киберпанк тема
- ✓ Установка "одной кнопкой"

---

**Наслаждайся кодингом в стиле киберпанк! 🎮⚡**
